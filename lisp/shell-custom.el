;;; shell-custom --- Shell (sh) customization

;;; Commentary:
;; Load this package with:
;; (require 'shell-custom)

;;; Code:

(add-to-list 'auto-mode-alist '("\\.bash_aliases\\'" . sh-mode))

(provide 'shell-custom)

;;; shell-custom.el ends here
