;;; gptel-custom --- gptel customization

;;; Commentary:
;; Load this package with:
;; (require 'gptel-custom)

;;; Code:

(require 'gptel)
(setq gptel-default-mode 'org-mode)
(provide 'gptel-custom)

;;; gptel-custom.el ends here
